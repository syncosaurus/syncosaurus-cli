// Authentication handlers are defined here in '../src/authHandler.js'

// Example authHandler function definition

// const authServerUrl = `https://www.myAuthServer.com`;

// export default async function authHandler(token) {
//   try {
//     const response = await fetch(`${authServerUrl}/verify?token=${token}`);
//     const { protectedHeader, payload } = response.json();

//     return { protectedHeader, payload };
//   } catch (error) {
//     console.error(error);
//   }
// }
